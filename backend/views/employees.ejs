<nav class="navbar navbar-expand-md navbar-dark bg-primary">
	<a class="navbar-brand" href="/dashboard">Cloetta</a>
	<button
		class="navbar-toggler"
		type="button"
		data-toggle="collapse"
		data-target="#navbarColor01"
		aria-controls="navbarColor01"
		aria-expanded="false"
		aria-label="Toggle navigation"
	>
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="/stores">Stores</a>
			</li>
			<li class="nav-item active">
				<a class="nav-link" href="/employees">Team</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="#">About</a>
			</li>
		</ul>
		<a href="/users/logout" class="btn btn-secondary">Logout</a>
	</div>
</nav>
<!-- Breadcrumbs -->
<ol class="breadcrumb">
	<li class="breadcrumb-item"><a href="/dashboard">Home</a></li>
	<li class="breadcrumb-item active">Team</li>
</ol>
<div class="col">
	<h1 class="mt-4">Employees</h1>
	<ul class="nav nav-tabs">
		<li class="nav-item">
			<a class="nav-link active" data-toggle="tab" href="#all">Alle Konsulenter</a>
		</li>
		<li class="nav-item">
			<a class="nav-link" data-toggle="tab" href="#service">Service Konsulenter</a>
		</li>
		<li class="nav-item">
			<a class="nav-link" data-toggle="tab" href="#sales">Salgs Konsulenter</a>
		</li>
	</ul>
	<div id="myTabContent" class="tab-content">
		<div class="tab-pane fade show active" id="all">
			<table class="table table-hover" id="myTable">
				<thead>
					<tr>
						<th>Name</th>
						<th>Title</th>
						<th>Email</th>
						<th>Address</th>
					</tr>
				</thead>
				<tbody>
					<% employees.map( emp => { %>
					<tr>
						<td><a href="/employees/emp_id=<%- emp._id -%>"><%= emp.name %></a></td>
						<td><%= emp.title %></td>
						<td><%= emp.email %></td>
						<td><%= emp.address %></td>
					</tr>
					<% }) %>
				</tbody>
			</table>
		</div>

		<div class="tab-pane fade" id="service">
			<table class="table table-hover" id="myTable">
				<thead>
					<tr>
						<th>Name</th>
						<th>Title</th>
						<th>Email</th>
						<th>Address</th>
					</tr>
				</thead>
				<tbody>
					<% employees.map( emp => { %> <% if(emp.title == 'Service Konsulent') {%>
					<tr>
						<td><a href="/employees/emp_id=<%- emp._id -%>"><%= emp.name %></a></td>
						<td><%= emp.title %></td>
						<td><%= emp.email %></td>
						<td><%= emp.address %></td>
					</tr>
					<% } %> <% }) %>
				</tbody>
			</table>
		</div>
		<div class="tab-pane fade" id="sales">
			<table class="table table-hover" id="myTable">
				<thead>
					<tr>
						<th>Name</th>
						<th>Title</th>
						<th>Email</th>
						<th>Address</th>
					</tr>
				</thead>
				<tbody>
					<% employees.map( emp => { %> <% if(emp.title == 'Salgs Konsulent') {%>
					<tr>
						<td><a href="/employees/emp_id=<%- emp._id -%>"><%= emp.name %></a></td>
						<td><%= emp.title %></td>
						<td><%= emp.email %></td>
						<td><%= emp.address %></td>
					</tr>
					<% } %> <% }) %>
				</tbody>
			</table>
		</div>
	</div>
</div>
